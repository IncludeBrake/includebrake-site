User-agent: *
Allow: /

# Serverless endpoints.
Disallow: /api/

# Retired offers (chatbot, authority, tractionbuild, intake) are NOT listed
# here on purpose: they 301 to /, and a crawler that is blocked from fetching
# them never sees the redirect, which leaves the dead URLs in the index.

Sitemap: https://www.includebrake.com/sitemap.xml
